# The port the MCP server will listen on
PORT=3000

# The origin for CORS requests. Use * for development.
CORS_ORIGIN=*

# The base URL of the Jotty server
JOTTY_BASE_URL=http://localhost:1122
JOTTY_API_KEY=ck_xxxxx

# A secret key to authenticate requests to the MCP server.
# You can generate a new key using a UUID generator.
API_KEY=$(openssl rand -base64 32)